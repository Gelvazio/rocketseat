import React from 'react';

import Routes from './src/routes';

//import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  return (
    <Routes />
  );
}


/*
const styles = StyleSheet.create({
  container: {
    display:"flex",         //Isso já é padrão no Mobile e não precisa declarar
    flexDirection:"column", //Isso já é padrão no Mobile e não precisa declarar
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center'
  },
});

*/
